// @vitest-environment node
import { zipSync } from "fflate/browser";
import { describe, expect, it } from "vitest";
import { convertFile, type ConvertOptions } from "../src/core/converter/convert";
import { targetsFor } from "../src/core/converter/matrix";
const enc=(s:string)=>new TextEncoder().encode(s);
function canvas():ConvertOptions{let w=1,h=1;const c={translate(){},rotate(){},scale(){},drawImage(){},getImageData:()=>({width:w,height:h,data:new Uint8ClampedArray(w*h*4),colorSpace:"srgb"})as ImageData};return{canvas:{canvasFactory:()=>({get width(){return w},set width(v:number){w=v},get height(){return h},set height(v:number){h=v},getContext:()=>c,toBlob(cb:(b:Blob|null)=>void,mime?:string){cb(new Blob([enc("RIFF0000WEBP")],{type:mime??"image/webp"}))}})as unknown as HTMLCanvasElement,decode:async()=>({width:10,height:10,close(){}})as ImageBitmap}}}
describe("round 2 batch 26: HTMLZ modern images",()=>{const bytes=zipSync({"index.html":enc("<!doctype html><html><body><h1>Local book</h1><p>Readable chapter.</p></body></html>")});it("advertises ranks 3091, 3095, 3097",()=>expect(targetsFor("htmlz")).toEqual(expect.arrayContaining(["image-gif","image-svg","image-webp"])));it.each([["image-gif","gif","image/gif","GIF89a"],["image-svg","svg","image/svg+xml","<svg"],["image-webp","webp","image/webp","RIFF"]]as const)("converts real HTMLZ to %s",async(target,ext,mime,sig)=>{const r=await convertFile({bytes,name:"book.htmlz"},target,canvas());expect(r).toMatchObject({name:`book.${ext}`,mime});expect(new TextDecoder().decode(r.bytes.slice(0,100))).toContain(sig)});it("rejects corrupt and HTML-free HTMLZ",async()=>{await expect(convertFile({bytes:enc("bad"),name:"bad.htmlz"},"image-svg")).rejects.toThrow(/htmlz|ZIP/i);await expect(convertFile({bytes:zipSync({"readme.txt":enc("text")}),name:"empty.htmlz"},"image-svg")).rejects.toThrow(/no HTML/i)});});
