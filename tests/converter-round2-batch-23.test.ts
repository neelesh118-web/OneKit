// @vitest-environment node
import { describe, expect, it } from "vitest";
import { convertFile, type ConvertOptions } from "../src/core/converter/convert";
import { targetsFor } from "../src/core/converter/matrix";
const enc=(s:string)=>new TextEncoder().encode(s);
function canvas():ConvertOptions{let w=1,h=1;const c={translate(){},rotate(){},scale(){},drawImage(){},getImageData:()=>({width:w,height:h,data:new Uint8ClampedArray(w*h*4),colorSpace:"srgb"})as ImageData};return{canvas:{canvasFactory:()=>({get width(){return w},set width(v:number){w=v},get height(){return h},set height(v:number){h=v},getContext:()=>c,toBlob(){}})as unknown as HTMLCanvasElement,decode:async()=>({width:10,height:10,close(){}})as ImageBitmap}}}
describe("round 2 batch 23: ABW to GIF",()=>{it("advertises rank 2798",()=>expect(targetsFor("abw")).toContain("image-gif"));it("converts real AbiWord XML to GIF",async()=>{const bytes=enc('<?xml version="1.0"?><abiword><section><p style="Heading 1">AbiWord report</p><p>Readable local content.</p></section></abiword>');const r=await convertFile({bytes,name:"report.abw"},"image-gif",canvas());expect(r).toMatchObject({name:"report.gif",mime:"image/gif"});expect(new TextDecoder().decode(r.bytes.slice(0,6))).toBe("GIF89a")});it("rejects malformed AbiWord content",async()=>{await expect(convertFile({bytes:enc("not xml"),name:"bad.abw"},"image-gif",canvas())).rejects.toThrow(/AbiWord|readable text/i)});});
